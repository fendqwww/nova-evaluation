import "server-only";

/**
 * Что импортирует остальное приложение, когда ему нужно основание обработки.
 *
 * `hasAiConsent` — единственная функция отсюда, которую вызывают AI-действия, и
 * вызывают её все пять без исключения. Согласие на трансграничную передачу —
 * это то, что делает обращение к Gemini законным; проверка «включён ли
 * переключатель» на этот вопрос не отвечает, потому что переключатель
 * настраивает удобство, а не право.
 */
export {
  getConsentState,
  recordConsents,
  revokeConsent,
  hasAiConsent,
} from "@/features/legal/server/consent.repository";
